upstream eoffice {
    
    
    server 10.0.5.11:8501 weight=1 max_fails=3 fail_timeout=10s;
    
    
    
    # Internal Bridge for 10.0.32.3:443
    server 127.0.0.1:10089 weight=1 max_fails=3 fail_timeout=10s backup;
    
    

    # No keepalive to ensure fresh connections identical to NPM native behavior
}

server {
    listen 8200;

    location / {
        # We always use http:// to the upstream block, because HTTPS upstreams 
        # are proxied through our internal HTTP-to-HTTPS bridges below.
        proxy_pass http://eoffice;

        # ── Failover & Retry Logic ──────────────────────────────
        proxy_next_upstream error timeout http_500 http_502 http_503 http_504;
        proxy_next_upstream_tries 3;
        proxy_next_upstream_timeout 5s;

        # ── Connection Timeouts ─────────────────────────────────
        proxy_connect_timeout 3s;
        proxy_read_timeout 10s;
        proxy_send_timeout 10s;

        # ── HTTP/1.1 Keepalive & Websockets ─────────────────────
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $http_connection;

        # -- Header Forwarding -----------------------------------------------
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $http_x_real_ip;
        proxy_set_header X-Forwarded-For $http_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $http_x_forwarded_proto;
        proxy_set_header X-Forwarded-Scheme $http_x_forwarded_scheme;

        # ── Buffer Settings ─────────────────────────────────────
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
    }
}






server {
    listen 127.0.0.1:10089;
    
    location / {
        proxy_pass https://10.0.32.3:443;
        proxy_ssl_verify off;
        proxy_http_version 1.1;
        
        # Pass headers through the bridge
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $http_x_forwarded_proto;
        proxy_set_header X-Forwarded-Scheme $http_x_forwarded_scheme;
    }
}
